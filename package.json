{
  "name": "news-radar",
  "version": "1.0.0",
  "description": "Daily news radar — QA/testing leadership + AI/agent tooling, collected via agent-reach",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "collect": "node scripts/reach-collect.mjs",
    "dev": "tsc && node dist/index.js",
    "all": "node scripts/reach-collect.mjs && tsc && node dist/index.js"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^25.9.3",
    "typescript": "^6.0.3"
  }
}
